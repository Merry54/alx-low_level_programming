#include <stdio.h>

/**
 * main- the program should print a number, followed by a new line
 * @argc: int  
 * @argv: list
 *return: 0
 */

int main (int argc, char const *argv[])
{
(void)argv;
printf(%i\n, argc -1);
return (0);
}
